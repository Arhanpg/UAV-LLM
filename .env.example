# Ollama (host-level service — not in Docker on Windows)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:4b

# LLM mode: live (real Ollama) | benchmark (Monte Carlo noise injection)
LLM_MODE=live

# SQLite persistence
DATABASE_URL=sqlite:///./data/uav_llm.db

# Backend
API_HOST=0.0.0.0
API_PORT=8000

# Frontend (dev proxy target)
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
